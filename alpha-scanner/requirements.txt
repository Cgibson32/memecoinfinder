# Core dependencies
aiohttp>=3.9.0
aiosqlite>=0.19.0
python-telegram-bot>=21.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
APScheduler>=3.10.0
streamlit>=1.30.0
plotly>=5.18.0

# Optional — install separately if needed
# pytrends>=4.9.0       # Google Trends (may have build issues on some systems)
# telethon>=1.34.0      # Telegram group monitoring
# feedparser>=6.0.0     # Twitter/Nitter RSS (may need sgmllib3k)
